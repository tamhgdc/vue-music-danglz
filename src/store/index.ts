import Vue from 'vue'
import Vuex from 'vuex'
import Tracks from './modules/tracks'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    Tracks
  }
})
